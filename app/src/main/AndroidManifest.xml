<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.lenovo.database_app">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainScreenActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Add Product Activity -->


        <!-- Edit Product Activity -->
        <activity
            android:name=".XML_parser"
            android:label="@string/title_activity_xml_parser"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".JSON_parser"
            android:label="@string/title_activity_json_parser"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".JSON_insert_data"
            android:label="@string/title_activity_json_insert_data"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>
